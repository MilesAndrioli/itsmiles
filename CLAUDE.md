Every time you write some content, make sure to use the humanizer skill you can find in ~/.agents/skills/humanizer

IMPORTANT: Use **PascalCase** for custom CSS classes to distinguish them from Tailwind utilities at a glance. BEM style for parts of the component (__) and for variations (--).
