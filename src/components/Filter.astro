<div id="app-filter" class="has-grid"></div>

<style>
    #app-filter {
        position: fixed;
        z-index: var(--filter-layer);
        inset: 0;
        pointer-events: none;

        &.has-LUT {
            mix-blend-mode: soft-light;
            background-color: color-mix(in oklab, black 40%, transparent);
            backdrop-filter: saturate(120%) hue-rotate(-4deg);
        }

        &.has-grid {
            --border-color: var(--color-iron);
            mix-blend-mode: color-dodge;

            background-image:
                linear-gradient(
                    to bottom,
                    var(--border-color) 1px,
                    transparent 0
                ),
                linear-gradient(
                    to right,
                    var(--border-color) 1px,
                    transparent 0
                );

            /*--tile-width: calc(var(--contained) / 2 - var(--padders));*/
            --tile-width: calc(var(--contained) / 2);

            background-size: var(--tile-width) 50dvh;
            background-position: 50vw top;

            mask-image: linear-gradient(black, black);
            mask-size: calc(var(--contained) + 2px) 100%;
            mask-position: center;
            mask-repeat: no-repeat;
        }
    }
</style>
