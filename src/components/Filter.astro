<div id="app-filter" class="has-grid"></div>

<style>
    #app-filter {
        position: fixed;
        z-index: var(--filter-layer);
        top: 0;
        left: 0;
        height: 100%;
        width: 100%;
        pointer-events: none;

        &.has-LUT {
            mix-blend-mode: soft-light;
            background-color: color-mix(in oklab, black 40%, transparent);
            backdrop-filter: saturate(120%) hue-rotate(-4deg);
        }

        &.has-grid {
            --border-color: var(--color-iron-400);
            mix-blend-mode: color-dodge;

            background-image:
                linear-gradient(
                    to bottom,
                    var(--border-color) 1px,
                    transparent 0
                ),
                linear-gradient(
                    to right,
                    var(--border-color) 1px,
                    transparent 0
                );

            background-size: calc(var(--contained) / 2) 50dvh;
            background-position: 50vw top;
        }
    }
</style>
