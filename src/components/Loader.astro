<div id="app-loader"></div>

<style>
    #app-loader {
        position: fixed;
        inset: 0;
        z-index: var(--loader-layer);
        background-color: var(--color-iron-950);
        transition: var(--in-out--super-slow);
    }

    html.Page--HasLoaded #app-loader {
        opacity: 0;
        visibility: hidden;
        pointer-events: none;
    }
</style>
